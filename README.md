# product-review-rating-prediction
NLP + structured-data pipeline predicting 1–5 star ratings from review text and product metadata for an e-commerce marketplace, using TF-IDF and an ablation study to show text signal dominates structured features (weighted F1 ~0.61).
